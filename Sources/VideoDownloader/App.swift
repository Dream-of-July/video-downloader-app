import SwiftUI

@main
struct VideoDownloaderApp: App {
    var body: some Scene {
        WindowGroup("视频下载器") {
            ContentView()
        }
        .defaultSize(width: 540, height: 700)
    }
}
